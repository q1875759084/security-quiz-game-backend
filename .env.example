# 复制此文件为 .env，填入本地开发的真实值
# .env 已被 .gitignore 排除，不会提交到仓库

# 服务端口
PORT=3000

# 允许跨域的前端地址（本地开发填 localhost，生产由 compose 注入真实域名）
CORS_ORIGIN=http://localhost:5173

# JWT 签名密钥（本地随意填写，生产必须使用强随机字符串）
JWT_SECRET=local_dev_secret_change_in_production
